# INDEX — /study

> Map-of-content. Keep current (PROJECT_BRIEF §2).

**Purpose:** The researcher's study system — the *learning* engine running beside the *research* corpus. Full design + rationale: `DESIGN.md`. Operating rules + templates: `RULES.md`. Both Tier 2.

The boundary in one line: **study material is never a verified source; study content earns its way into the corpus via promotion (fresh from verified sources), never by leaking.** Firewalls: `RULES.md` §10.

## Contents

| item | what |
|---|---|
| `DESIGN.md` | integration design v1.3 (Tier 2) — the loop, conventions, assessment layer, tooling |
| `RULES.md` | operating rules: language, frontmatter, tag registry, cadences, quiz modes, schema (Tier 2) |
| `skills.md` | skill map — seeded from P1 exit criteria, all `untested` pending first weekly review |
| `questions.md` | study→research trigger ledger (empty) |
| `comparative-table.md` | working table; promotes to `/comparative/` (empty) |
| `错题本.md` | error-log view over `cases/` (empty) |
| `plan/` | study-plan documents 00–07 (the researcher's 8-file set, received 2026-07-28) |
| `templates/` | daily, prediction, case, literature-note |
| `journal/` | `daily/` + `predictions/` (empty — habit starts now) |
| `cases/` | real-world case bank, longitudinal (empty) |
| `examples/` | worked-example reference bank, provenance-tagged (empty) |
| `quizzes/` | generated quizzes + results; training-drill sets from `slice1-train-*` land here |
| `reviews/weekly/` | weekly review records (none yet) |
| `systems/` | per-card/十神/hexagram/decan records: `tarot/ bazi/ astrology/ iching/ lenormand/` (empty) |

Root `STATUS.md` is the two-engine dashboard (generated; archived to `/log/status-archive/`).
