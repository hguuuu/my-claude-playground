# INDEX — /log

> Map-of-content stub. Keep current: this is how sessions orient without reading everything (PROJECT_BRIEF §2).

**Purpose:** ADR-style session records + research journal.

Subdirs: `decisions/` (one file per significant decision), `sessions/` (one entry per session). File: `dead-ends.md` (negative results — high value, never silently dropped).

## Contents

_(empty — populated as work lands here)_
