---
id: session-0007-2026-07-28-meta-prefix
lang: en
type: log
session_type: meta
task: add `meta:` session type (researcher-approved constitution amendment)
date: 2026-07-28
tier: 2
status: ready for review
---

# Session 0007 — meta: prefix added to the session-type taxonomy

**Session type:** `meta:` — its own first use.

## What

Researcher proposed adding prefixes (research/planning/organization/…); recommendation was to add exactly one — `meta:` — and researcher approved. Amended:

- `CLAUDE.md` session-type list: `meta:` defined (work ON the system: methodology specs, queue grooming, INDEX/log housekeeping, schema/constitution amendments; no research claims enter the corpus under it; tier follows touched surface).
- `PROJECT_BRIEF.md` §8 (same definition, marked as researcher decision of 2026-07-28) and §9 prefix list (now includes `study:`, `meta:`).

## Why (decision rationale, recorded)

- A prefix must carry **rules**, not a topic — otherwise it's taxonomy bloat. `meta:` carries two: (1) no research claims may enter the corpus under it; (2) tier by touched surface.
- Observed gap: sessions 0006 (reliability metric), the log renumbering, and the study design sessions were stamped `engineering:` without touching the formal layer — diluting `engineering:`'s "tests mandatory, formal layer" meaning. Queue task IDs (`meta-001/002`) already used the name.
- Rejected: `research:` (redundant — phase0/intake/synthesis/audit ARE the research prefixes; a generic label would swallow them), separate `planning:`/`organization:` (both are `meta:` in substance; boundary between them invites misclassification for zero rule difference — split later only if `meta:` overloads).
- Taxonomy now considered complete at 7: five research verbs + `study:` + `meta:`. Future candidates must answer: what rule does it carry that these don't?
- **No retro-tagging**: history stays as committed; this log records the mapping precedent instead.

## Next

- Use `meta:` for future queue grooming, INDEX/log housekeeping, methodology work.
- Tier-2 (constitution + brief touched): **ready for review.**
