# INDEX — /sources

> Map-of-content. Keep current: this is how sessions orient without reading everything (PROJECT_BRIEF §2).

**Purpose:** Verified primary texts (ctext dumps, scans, researcher-provided). The citation-verification backstop.

A classical passage may NEVER be cited unless a verified copy lives here (§1.2).

## Structure

- **Primary-text library (this directory):** verified copies only — reliability grade A by definition. _(empty — populated via intake, gated on phase0-002)_
- `RELIABILITY.md` — the source-reliability metric: grade every source we touch (level × access × flags); source-level labels are independent of claim-level `verification` status.
- `register/` — the encounter register: EVERY source we run into gets recorded here with a label, including unconfirmed ones (they may align with future research). Registration ≠ endorsement.

## Contents

_(no verified primary texts yet)_

_Note: phase0-002 ran once (s0010, 2026-07-28) producing an obtainability survey + 6 intake task definitions, but the researcher withdrew that output from review pending a possible deep-research redo — it lives only in git history (branch `claude/columbia-spirituality-program-ny7ccd`, commit `8a9ccec`), is **not** part of the working tree, and must not be cited or built on. The task is back to `todo` in `/queue/`._
